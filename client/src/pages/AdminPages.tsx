/** Módulos operacionais persistentes com diálogos internos do Nexus Stream. */
import { useMemo, useState } from "react";
import { CalendarDays, CheckCircle2, CircleAlert, Clock3, Copy, FileCheck2, ListFilter, MoreHorizontal, Plus, RadioTower, Search, Settings2, ShieldCheck, UsersRound } from "lucide-react";
import { toast } from "sonner";
import { useLocation } from "wouter";
import { useAuth } from "@/_core/hooks/useAuth";
import PanelLayout from "@/components/PanelLayout";
import { ChannelFormDialog } from "@/components/ChannelFormDialog";
import { PanelAlertDialog, PanelConfirmDialog, PanelFormDialog } from "@/components/PanelActionDialog";
import { getSubscriptionSignal } from "@/lib/panelModel";
import { trpc } from "@/lib/trpc";

function daysUntil(date: Date | string) { return Math.ceil((new Date(date).getTime() - Date.now()) / 86_400_000); }

function PageHero({ eyebrow, title, description, actionLabel, onAction, disabled, icon: Icon }: { eyebrow: string; title: string; description: string; actionLabel: string; onAction: () => void; disabled?: boolean; icon: typeof UsersRound }) {
  return <section className="relative overflow-hidden rounded-[26px] border border-white/[0.08] bg-[#10191f] px-6 py-7 sm:px-8"><div className="pointer-events-none absolute -right-20 -top-20 h-56 w-56 rounded-full bg-[#43E6C2]/[0.06] blur-3xl" /><div className="relative flex flex-col justify-between gap-5 md:flex-row md:items-end"><div className="max-w-2xl"><div className="flex items-center gap-2"><span className="signal-dot" /><span className="font-mono text-[10px] uppercase tracking-[0.18em] text-[#9df6df]">{eyebrow}</span></div><h1 className="mt-4 text-3xl font-extrabold tracking-[-0.06em] text-white sm:text-4xl">{title}</h1><p className="mt-3 text-sm leading-6 text-slate-300">{description}</p></div><button type="button" disabled={disabled} onClick={onAction} className="pressable inline-flex shrink-0 items-center justify-center gap-2 rounded-xl bg-[#43E6C2] px-4 py-3 text-sm font-bold text-[#07201c] transition hover:bg-[#72f0d5] disabled:cursor-wait disabled:opacity-60"><Icon className="h-4 w-4" /> {actionLabel}</button></div></section>;
}

function DataFeedback({ loading, error, empty, children }: { loading: boolean; error?: unknown; empty: boolean; children: React.ReactNode }) {
  if (loading) return <div className="px-6 py-12 text-center font-mono text-xs uppercase tracking-[0.14em] text-slate-500">Carregando registros…</div>;
  if (error) return <div className="px-6 py-12 text-center"><CircleAlert className="mx-auto h-5 w-5 text-[#ffbb4d]" /><p className="mt-3 text-sm font-semibold text-slate-200">Não foi possível carregar os dados.</p><p className="mt-1 text-xs text-slate-500">Verifique a conexão do banco e tente novamente.</p></div>;
  if (empty) return <div className="px-6 py-12 text-center"><FileCheck2 className="mx-auto h-6 w-6 text-slate-600" /><p className="mt-3 text-sm font-semibold text-slate-300">Nenhum registro criado ainda</p><p className="mt-1 text-xs text-slate-500">Use a ação principal desta tela para iniciar a operação.</p></div>;
  return <>{children}</>;
}

function SignalPill({ days, forcedStatus }: { days: number; forcedStatus?: "active" | "attention" | "expired" }) {
  const signal = forcedStatus === "attention" ? { tone: "attention" as const, label: "Próxima do vencimento" } : forcedStatus === "expired" ? { tone: "expired" as const, label: "Vencida" } : getSubscriptionSignal(days);
  const style = signal.tone === "stable" ? "border-[#43E6C2]/20 bg-[#43E6C2]/[0.07] text-[#9df6df]" : signal.tone === "attention" ? "border-[#ffbb4d]/20 bg-[#ffbb4d]/[0.07] text-[#ffd28b]" : "border-[#ff6b5b]/20 bg-[#ff6b5b]/[0.07] text-[#ffaaa2]";
  return <span className={`inline-flex rounded-full border px-2.5 py-1 font-mono text-[9px] uppercase tracking-[0.12em] ${style}`}>{signal.label}</span>;
}

export function CustomersPage() {
  const utils = trpc.useUtils();
  const [query, setQuery] = useState("");
  const [customerDialog, setCustomerDialog] = useState(false);
  const [statusTarget, setStatusTarget] = useState<{ id: number; label: string; status: "active" | "attention" | "expired" } | null>(null);
  const [activationToken, setActivationToken] = useState<string | null>(null);
  const customersQuery = trpc.customers.list.useQuery();
  const plansQuery = trpc.plans.list.useQuery();
  const createCustomer = trpc.customers.create.useMutation({ onSuccess: async result => { setCustomerDialog(false); setActivationToken(result.accessToken); await utils.customers.list.invalidate(); toast.success("Cliente criado com sucesso."); }, onError: error => toast.error(error.message) });
  const setStatus = trpc.customers.setStatus.useMutation({ onSuccess: () => { setStatusTarget(null); utils.customers.list.invalidate(); }, onError: error => toast.error(error.message) });
  const customers = customersQuery.data ?? [];
  const planOptions = (plansQuery.data ?? []).flatMap(plan => plan.cycles.filter(cycle => plan.isActive && cycle.isActive).map(cycle => ({ value: `${plan.id}:${cycle.id}`, label: `${plan.name} · ${cycle.cycle} · ${cycle.discountPercent}% desconto` })));
  const visible = useMemo(() => customers.filter(customer => customer.label.toLowerCase().includes(query.toLowerCase())), [customers, query]);
  const nextStatus = (status: "active" | "attention" | "expired") => status === "active" ? "attention" : status === "attention" ? "expired" : "active";
  return <PanelLayout><PageHero eyebrow="Base de clientes" title="Acompanhe acesso, ciclo e exceções." description="Clientes finais não acessam este painel. A operação controla limites de tela, vencimento e suspensão sem expor credenciais ao aplicativo." actionLabel="Adicionar cliente" onAction={() => setCustomerDialog(true)} icon={Plus} />
    <section className="mt-6 grid gap-3 sm:grid-cols-3">{[{ label: "Base monitorada", value: customers.length.toString().padStart(2, "0"), icon: UsersRound }, { label: "Próximos do vencimento", value: customers.filter(item => item.status === "attention" || (item.status === "active" && daysUntil(item.expiresAt) <= 3)).length.toString().padStart(2, "0"), icon: Clock3 }, { label: "Acesso suspenso", value: customers.filter(item => item.status === "expired").length.toString().padStart(2, "0"), icon: CircleAlert }].map(stat => { const Icon = stat.icon; return <article className="metric-card" key={stat.label}><Icon className="h-4 w-4 text-[#43E6C2]" /><p className="mt-7 font-mono text-[10px] uppercase tracking-[0.15em] text-slate-500">{stat.label}</p><p className="mt-1 text-3xl font-extrabold tracking-[-0.06em] text-white">{stat.value}</p></article>; })}</section>
    <section className="surface-card mt-6 overflow-hidden"><div className="flex flex-col gap-4 border-b border-white/[0.07] px-5 py-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Lista de controle</p><h2 className="mt-1 text-lg font-bold tracking-tight text-white">Clientes cadastrados</h2></div><div className="flex items-center gap-2 rounded-xl border border-white/[0.08] bg-black/10 px-3 py-2 sm:w-[270px]"><Search className="h-4 w-4 text-slate-500" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Buscar conta" className="w-full bg-transparent text-sm text-slate-100 outline-none placeholder:text-slate-600" /></div></div><DataFeedback loading={customersQuery.isLoading} error={customersQuery.error} empty={!customers.length}><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead className="border-b border-white/[0.06] bg-white/[0.018]"><tr className="font-mono text-[9px] uppercase tracking-[0.14em] text-slate-500"><th className="px-5 py-3.5 font-medium sm:px-6">Cliente</th><th className="px-5 py-3.5 font-medium">Plano</th><th className="px-5 py-3.5 font-medium">Telas</th><th className="px-5 py-3.5 font-medium">Status</th><th className="px-5 py-3.5 font-medium">Vencimento</th><th className="px-5 py-3.5 font-medium" /></tr></thead><tbody className="divide-y divide-white/[0.06]">{visible.map(customer => <tr key={customer.id} className="group text-sm text-slate-300 transition hover:bg-white/[0.025]"><td className="px-5 py-4 font-semibold text-slate-100 sm:px-6">{customer.label}</td><td className="px-5 py-4 text-slate-400">{customer.plan}</td><td className="px-5 py-4 font-mono text-xs text-slate-400">{customer.usedScreens} / {customer.screenLimit} telas</td><td className="px-5 py-4"><SignalPill days={daysUntil(customer.expiresAt)} forcedStatus={customer.status} /></td><td className="px-5 py-4 text-slate-500">{new Date(customer.expiresAt).toLocaleDateString("pt-BR")}</td><td className="px-5 py-4"><button type="button" onClick={() => setStatusTarget({ id: customer.id, label: customer.label, status: customer.status })} className="rounded-lg p-1.5 text-slate-500 transition hover:bg-white/[0.06] hover:text-white" title="Alterar estado da assinatura"><MoreHorizontal className="h-4 w-4" /></button></td></tr>)}</tbody></table></div></DataFeedback></section>
    <PanelFormDialog open={customerDialog} onOpenChange={setCustomerDialog} title="Adicionar cliente" description="Cadastre os dados do perfil e escolha o plano. Telas, período de teste e vencimento serão aplicados automaticamente." fields={[{ name: "label", label: "Nome completo", placeholder: "Ex.: Maria da Silva", wide: true }, { name: "email", label: "E-mail", type: "email", placeholder: "maria@exemplo.com" }, { name: "phone", label: "Telefone", placeholder: "(11) 99999-9999" }, { name: "planCycle", label: "Plano e ciclo", defaultValue: planOptions[0]?.value ?? "", options: planOptions, wide: true }]} submitLabel="Criar cliente" submitting={createCustomer.isPending} onSubmit={values => { const [planId, planCycleId] = values.planCycle.split(":").map(Number); if (!planId || !planCycleId) { toast.error("Cadastre um plano e ciclo antes de criar o cliente."); return; } createCustomer.mutate({ label: values.label.trim(), email: values.email.trim() || null, phone: values.phone.trim() || null, planId, planCycleId }); }} />
    <PanelConfirmDialog open={Boolean(statusTarget)} onOpenChange={open => { if (!open) setStatusTarget(null); }} title="Alterar estado de assinatura" description={statusTarget ? `Confirmar novo estado para ${statusTarget.label}: ${nextStatus(statusTarget.status)}.` : ""} confirmLabel="Confirmar alteração" submitting={setStatus.isPending} onConfirm={() => statusTarget && setStatus.mutate({ id: statusTarget.id, status: nextStatus(statusTarget.status) })} />
    <PanelAlertDialog open={Boolean(activationToken)} onOpenChange={open => { if (!open) setActivationToken(null); }} title="Token de ativação criado" description="Copie este token e entregue-o ao cliente uma única vez. O painel guarda apenas o hash para validar o aplicativo." secret={activationToken} onCopy={async () => { if (activationToken) { await navigator.clipboard.writeText(activationToken); toast.success("Token copiado."); } }} />
  </PanelLayout>;
}

export function ChannelsPage() {
  const utils = trpc.useUtils(); const [dialog, setDialog] = useState(false); const channelsQuery = trpc.channels.list.useQuery(); const createChannel = trpc.channels.create.useMutation({ onSuccess: () => { setDialog(false); utils.channels.list.invalidate(); toast.success("Canal criado como pausado."); }, onError: error => toast.error(error.message) }); const toggleChannel = trpc.channels.toggle.useMutation({ onSuccess: () => utils.channels.list.invalidate(), onError: error => toast.error(error.message) }); const channels = channelsQuery.data ?? [];
  return <PanelLayout><PageHero eyebrow="Orquestração de live TV" title="Canais com rotas, qualidade e fallback." description="A operação mantém as rotas e qualidades publicadas. URLs de origem ficam apenas no servidor." actionLabel="Criar canal" onAction={() => setDialog(true)} icon={Plus} /><section className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_330px]"><article className="surface-card overflow-hidden"><div className="flex items-center justify-between border-b border-white/[0.07] px-5 py-5 sm:px-6"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Canais persistidos</p><h2 className="mt-1 text-lg font-bold tracking-tight text-white">Matriz de distribuição</h2></div><span className="inline-flex items-center gap-2 rounded-lg border border-white/[0.08] px-3 py-2 text-xs font-semibold text-slate-400"><ListFilter className="h-3.5 w-3.5" /> {channels.length} canal(is)</span></div><DataFeedback loading={channelsQuery.isLoading} error={channelsQuery.error} empty={!channels.length}><div className="divide-y divide-white/[0.06]">{channels.map(channel => <div key={channel.id} className="flex flex-wrap items-center gap-x-5 gap-y-3 px-5 py-4 sm:px-6"><div className={`grid h-10 w-10 place-items-center rounded-xl border ${channel.isActive ? "border-[#43E6C2]/20 bg-[#43E6C2]/[0.08]" : "border-white/[0.08] bg-white/[0.035]"}`}>{channel.logoUrl ? <img src={channel.logoUrl} alt="" className="h-6 w-6 rounded object-contain" /> : <RadioTower className={`h-4 w-4 ${channel.isActive ? "text-[#43E6C2]" : "text-slate-500"}`} />}</div><div className="min-w-[155px] flex-1"><p className="text-sm font-bold text-slate-100">{channel.channelNumber} · {channel.name}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.12em] text-slate-500">{channel.groupTitle} · {channel.qualities} · {channel.ageRating === 0 ? "Livre" : `${channel.ageRating}+`}</p></div><div className="min-w-[90px]"><p className="font-mono text-[9px] uppercase tracking-[0.12em] text-slate-500">Rotas</p><p className="mt-1 text-sm text-slate-200">{channel.routeCount} qualidade(s)</p></div><button type="button" disabled={toggleChannel.isPending} onClick={() => toggleChannel.mutate({ id: channel.id, isActive: !channel.isActive })} className={`pressable rounded-full border px-3 py-1.5 font-mono text-[9px] uppercase tracking-[0.12em] ${channel.isActive ? "border-[#43E6C2]/20 bg-[#43E6C2]/[0.07] text-[#9df6df]" : "border-white/[0.1] bg-white/[0.035] text-slate-500"}`}>{channel.isActive ? "Publicado" : "Pausado"}</button></div>)}</div></DataFeedback></article><aside className="surface-card p-5"><Settings2 className="h-5 w-5 text-[#43E6C2]" /><p className="mt-6 font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Proteção de origem</p><h2 className="mt-1 text-lg font-bold tracking-tight text-white">Manifest proxy</h2><p className="mt-4 text-sm leading-6 text-slate-400">O cadastro suporta playlist mestre inspecionável ou qualidades separadas, com Origin, Referer e fallback independentes por link.</p></aside></section><ChannelFormDialog open={dialog} onOpenChange={setDialog} submitting={createChannel.isPending} onSubmit={input => createChannel.mutate(input)} /></PanelLayout>;
}

export function EpgPage() {
  const utils = trpc.useUtils();
  const [dialog, setDialog] = useState(false);
  const sourcesQuery = trpc.epg.list.useQuery();
  const createSource = trpc.epg.create.useMutation({ onSuccess: () => { setDialog(false); utils.epg.list.invalidate(); toast.success("Fonte EPG adicionada."); }, onError: error => toast.error(error.message) });
  const syncNow = trpc.epg.syncNow.useMutation({ onSuccess: result => { utils.epg.list.invalidate(); toast.success(result.result === "synced" ? "Programação XMLTV atualizada." : "A cobertura ainda é suficiente; nenhuma nova leitura era necessária."); }, onError: error => { utils.epg.list.invalidate(); toast.error(error.message); } });
  const sources = sourcesQuery.data ?? [];
  return <PanelLayout><PageHero eyebrow="Guia de programação" title="Fontes XMLTV com sincronização inteligente." description="O painel só relê a fonte quando a cobertura futura estiver baixa, ausente ou após uma falha de leitura." actionLabel="Adicionar fonte" onAction={() => setDialog(true)} icon={Plus} /><section className="mt-6 grid gap-3 sm:grid-cols-3">{[{ label: "Programas indexados", value: sources.reduce((total, item) => total + item.programmeCount, 0).toLocaleString("pt-BR"), icon: FileCheck2 }, { label: "Fontes ativas", value: sources.filter(item => item.status === "healthy").length.toString().padStart(2, "0"), icon: RadioTower }, { label: "Fontes totais", value: sources.length.toString().padStart(2, "0"), icon: Clock3 }].map(stat => { const Icon = stat.icon; return <article className="metric-card" key={stat.label}><Icon className="h-4 w-4 text-[#43E6C2]" /><p className="mt-7 font-mono text-[10px] uppercase tracking-[0.15em] text-slate-500">{stat.label}</p><p className="mt-1 text-3xl font-extrabold tracking-[-0.06em] text-white">{stat.value}</p></article>; })}</section><section className="surface-card mt-6 overflow-hidden"><div className="border-b border-white/[0.07] px-5 py-5 sm:px-6"><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Origem de dados</p><h2 className="mt-1 text-lg font-bold tracking-tight text-white">Fontes XMLTV</h2></div><DataFeedback loading={sourcesQuery.isLoading} error={sourcesQuery.error} empty={!sources.length}><div className="divide-y divide-white/[0.06]">{sources.map(source => <div key={source.id} className="flex flex-wrap items-center gap-4 px-5 py-5 sm:px-6"><div className={`h-2 w-2 rounded-full ${source.status === "healthy" ? "bg-[#43E6C2]" : source.status === "attention" ? "bg-[#ffbb4d]" : "bg-slate-600"}`} /><div className="min-w-[180px] flex-1"><p className="text-sm font-bold text-slate-100">{source.name}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.12em] text-slate-500">{source.programmeCount.toLocaleString("pt-BR")} programas · limiar {source.refreshThresholdHours}h</p>{source.coverageEndsAt ? <p className="mt-1 text-xs text-slate-400">Cobertura até {new Date(source.coverageEndsAt).toLocaleString("pt-BR")}</p> : null}{source.lastError ? <p className="mt-1 text-xs text-[#ffd28b]">Última falha: {source.lastError}</p> : null}</div><p className="text-sm text-slate-400">{source.lastSyncedAt ? new Date(source.lastSyncedAt).toLocaleString("pt-BR") : "Ainda não sincronizada"}</p><button type="button" disabled={syncNow.isPending} onClick={() => syncNow.mutate({ id: source.id })} className="pressable rounded-xl border border-white/[0.1] bg-white/[0.035] px-3 py-2 text-xs font-bold text-slate-200">Ler agora</button></div>)}</div></DataFeedback></section><PanelFormDialog open={dialog} onOpenChange={setDialog} title="Adicionar fonte XMLTV" description="Informe a URL XMLTV e o limiar mínimo de programação futura antes de uma nova leitura automática." fields={[{ name: "name", label: "Nome da fonte", placeholder: "Ex.: XMLTV Principal", wide: true }, { name: "feedUrl", label: "URL XMLTV", placeholder: "https://servidor.exemplo/epg.xml", wide: true }, { name: "refreshThresholdHours", label: "Cobertura mínima (horas)", type: "number", defaultValue: "6", placeholder: "6" }]} submitLabel="Adicionar fonte" submitting={createSource.isPending} onSubmit={values => createSource.mutate({ name: values.name.trim(), feedUrl: values.feedUrl.trim(), refreshThresholdHours: Number(values.refreshThresholdHours) })} /></PanelLayout>;
}

export function SubscriptionsPage() {
  const { user } = useAuth(); const [, setLocation] = useLocation(); const customersQuery = trpc.customers.list.useQuery(); const [billingTarget, setBillingTarget] = useState<{ id: number; label: string } | null>(null); const [pixPreview, setPixPreview] = useState<{ providerPaymentId: string; qrCode: string | null; qrCodeBase64: string | null; ticketUrl: string | null } | null>(null); const createPix = trpc.billing.createPix.useMutation({ onSuccess: payment => { setBillingTarget(null); setPixPreview(payment); toast.success("Cobrança PIX criada e registrada."); }, onError: error => toast.error(error.message) }); const customers = customersQuery.data ?? [];
  const copyPix = async () => { if (!pixPreview?.qrCode) return; await navigator.clipboard.writeText(pixPreview.qrCode); toast.success("Código PIX copiado."); };
  return <PanelLayout><PageHero eyebrow="Cobrança recorrente" title="Assinaturas controladas por PIX no aplicativo." description="O painel determina o estado de cada assinatura; o aplicativo exibe QR Code, código PIX e alertas de vencimento ou bloqueio." actionLabel="Configurar gateway" onAction={() => setLocation("/integracoes")} icon={Copy} /><section className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_330px]"><article className="surface-card overflow-hidden"><div className="border-b border-white/[0.07] px-5 py-5 sm:px-6"><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Ciclo de cobrança</p><h2 className="mt-1 text-lg font-bold tracking-tight text-white">Estados aplicáveis ao aplicativo</h2></div><DataFeedback loading={customersQuery.isLoading} error={customersQuery.error} empty={!customers.length}><div className="divide-y divide-white/[0.06]">{customers.map(customer => { const days = daysUntil(customer.expiresAt); const signal = getSubscriptionSignal(days); return <div key={customer.id} className="flex flex-wrap items-center gap-4 px-5 py-5 sm:px-6"><div className={`grid h-10 w-10 place-items-center rounded-xl border ${customer.status === "active" ? "border-[#43E6C2]/20 bg-[#43E6C2]/[0.08]" : customer.status === "attention" ? "border-[#ffbb4d]/20 bg-[#ffbb4d]/[0.08]" : "border-[#ff6b5b]/20 bg-[#ff6b5b]/[0.08]"}`}><CalendarDays className="h-4 w-4 text-[#43E6C2]" /></div><div className="min-w-[150px] flex-1"><p className="text-sm font-bold text-slate-100">{customer.label}</p><p className="mt-1 text-xs text-slate-500">{customer.plan} · {signal.description}</p></div><SignalPill days={days} forcedStatus={customer.status} /><button type="button" disabled={user?.role !== "admin"} onClick={() => setBillingTarget({ id: customer.id, label: customer.label })} className="pressable rounded-xl border border-white/[0.1] bg-white/[0.035] px-3 py-2 text-xs font-bold text-slate-200 disabled:cursor-not-allowed disabled:opacity-50">Gerar PIX</button></div>; })}</div></DataFeedback></article><aside className="surface-card p-5"><ShieldCheck className="h-5 w-5 text-[#43E6C2]" /><p className="mt-6 font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Contrato para o app</p><h2 className="mt-1 text-lg font-bold tracking-tight text-white">Alertas sem login no painel</h2><div className="mt-5 space-y-4">{["Ativa: acesso permitido, sem alerta.", "Próxima do vencimento: popup com CTA para pagar.", "Vencida: acesso suspenso e QR Code PIX disponível."].map(item => <div className="flex gap-3" key={item}><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-[#43E6C2]" /><p className="text-sm leading-5 text-slate-300">{item}</p></div>)}</div>{pixPreview ? <div className="mt-6 rounded-xl border border-[#43E6C2]/15 bg-[#43E6C2]/[0.04] p-3"><p className="font-mono text-[9px] uppercase tracking-[0.13em] text-[#9df6df]">Cobrança {pixPreview.providerPaymentId}</p>{pixPreview.qrCodeBase64 ? <img src={`data:image/png;base64,${pixPreview.qrCodeBase64}`} alt="QR Code PIX" className="mx-auto mt-3 h-36 w-36 rounded-lg bg-white p-2" /> : null}{pixPreview.qrCode ? <button type="button" onClick={copyPix} className="pressable mt-3 inline-flex w-full items-center justify-center gap-2 rounded-lg border border-[#43E6C2]/20 px-3 py-2 text-xs font-bold text-[#9df6df]"><Copy className="h-3.5 w-3.5" /> Copiar código PIX</button> : null}</div> : <div className="mt-6 rounded-xl border border-[#ffbb4d]/15 bg-[#ffbb4d]/[0.06] p-3"><p className="font-mono text-[9px] uppercase tracking-[0.13em] text-[#ffd28b]">Configure antes de cobrar</p><p className="mt-1.5 text-xs leading-5 text-slate-400">Cadastre o token e a URL do Mercado Pago na área de integrações administrativas.</p></div>}</aside></section><PanelFormDialog open={Boolean(billingTarget)} onOpenChange={open => { if (!open) setBillingTarget(null); }} title="Gerar cobrança PIX" description={billingTarget ? `Informe o e-mail pagador para ${billingTarget.label}. O QR Code será retornado pelo gateway configurado.` : ""} fields={[{ name: "payerEmail", label: "E-mail pagador", type: "email", placeholder: "cliente@exemplo.com" }, { name: "amount", label: "Valor (R$)", type: "number", defaultValue: "24.90" }]} submitLabel="Gerar PIX" submitting={createPix.isPending} onSubmit={values => billingTarget && createPix.mutate({ customerId: billingTarget.id, payerEmail: values.payerEmail.trim(), amountCents: Math.round(Number(values.amount.replace(",", ".")) * 100), description: `Assinatura Nexus Stream · ${billingTarget.label}` })} /></PanelLayout>;
}
