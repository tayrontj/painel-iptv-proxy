import { useState } from "react";
import { BadgePercent, CalendarClock, MonitorUp, Plus } from "lucide-react";
import { toast } from "sonner";
import PanelLayout from "@/components/PanelLayout";
import { PanelFormDialog } from "@/components/PanelActionDialog";
import { trpc } from "@/lib/trpc";

export default function PlansPage() {
  const utils = trpc.useUtils(); const [open, setOpen] = useState(false); const plansQuery = trpc.plans.list.useQuery();
  const createPlan = trpc.plans.create.useMutation({ onSuccess: () => { setOpen(false); utils.plans.list.invalidate(); toast.success("Plano multiciclo criado."); }, onError: error => toast.error(error.message) });
  const plans = plansQuery.data ?? [];
  return <PanelLayout><section className="relative overflow-hidden rounded-[26px] border border-white/[0.08] bg-[#10191f] px-6 py-7 sm:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-[#9df6df]">Catálogo de planos</p><h1 className="mt-4 text-3xl font-extrabold tracking-[-0.06em] text-white sm:text-4xl">Um plano, vários ciclos e descontos.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Defina o preço mensal, as telas e o teste uma vez. Em seguida, configure os descontos por ciclo dentro do próprio plano.</p></div><button type="button" onClick={() => setOpen(true)} className="pressable inline-flex items-center gap-2 rounded-xl bg-[#43E6C2] px-4 py-3 text-sm font-bold text-[#07201c]"><Plus className="h-4 w-4" /> Criar plano</button></div></section><section className="mt-6 grid gap-4 lg:grid-cols-2">{plansQuery.isLoading ? <div className="surface-card p-8 text-sm text-slate-500">Carregando planos…</div> : plans.length ? plans.map(plan => <article key={plan.id} className="surface-card p-5"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-slate-500">Plano base</p><h2 className="mt-1 text-xl font-bold text-white">{plan.name}</h2><p className="mt-2 text-sm text-slate-400">R$ {(plan.monthlyPriceCents / 100).toFixed(2).replace(".", ",")} / mensal</p></div><span className="rounded-full border border-[#43E6C2]/20 bg-[#43E6C2]/[0.07] px-2.5 py-1 font-mono text-[9px] uppercase tracking-[0.13em] text-[#9df6df]">{plan.screenLimit} tela(s)</span></div><div className="mt-5 grid gap-2">{plan.cycles.map(cycle => <div key={cycle.id} className="flex items-center justify-between rounded-xl border border-white/[0.07] bg-white/[0.025] px-3 py-2.5"><span className="text-sm font-semibold text-slate-200">{cycle.cycle} · {cycle.intervalDays} dias</span><span className="font-mono text-xs text-[#9df6df]">{cycle.discountPercent}% desconto</span></div>)}</div><div className="mt-4 flex gap-4 text-xs text-slate-500"><span className="inline-flex items-center gap-1.5"><CalendarClock className="h-3.5 w-3.5" /> {plan.trialDays} dias de teste</span><span className="inline-flex items-center gap-1.5"><MonitorUp className="h-3.5 w-3.5" /> {plan.screenLimit} telas</span></div></article>) : <div className="surface-card p-10 text-center lg:col-span-2"><BadgePercent className="mx-auto h-6 w-6 text-slate-600" /><p className="mt-3 text-sm font-bold text-slate-300">Nenhum plano configurado</p><p className="mt-1 text-xs text-slate-500">Crie o primeiro plano para liberar o cadastro de clientes.</p></div>}</section><PanelFormDialog open={open} onOpenChange={setOpen} title="Criar plano multiciclo" description="Os percentuais se aplicam sobre o preço mensal de referência. Os ciclos mensal, trimestral, semestral e anual serão criados no mesmo plano." fields={[{ name: "name", label: "Nome do plano", placeholder: "Ex.: Premium" }, { name: "monthlyPrice", label: "Preço mensal (R$)", type: "number", defaultValue: "24.90" }, { name: "screens", label: "Quantidade de telas", type: "number", defaultValue: "1" }, { name: "trialDays", label: "Período de teste (dias)", type: "number", defaultValue: "0" }, { name: "quarterlyDiscount", label: "Desconto trimestral (%)", type: "number", defaultValue: "5" }, { name: "semiannualDiscount", label: "Desconto semestral (%)", type: "number", defaultValue: "10" }, { name: "annualDiscount", label: "Desconto anual (%)", type: "number", defaultValue: "20" }]} submitLabel="Salvar plano" submitting={createPlan.isPending} onSubmit={values => createPlan.mutate({ name: values.name.trim(), monthlyPriceCents: Math.round(Number(values.monthlyPrice.replace(",", ".")) * 100), screenLimit: Number(values.screens), trialDays: Number(values.trialDays), cycles: [{ cycle: "monthly", intervalDays: 30, discountPercent: 0 }, { cycle: "quarterly", intervalDays: 90, discountPercent: Number(values.quarterlyDiscount) }, { cycle: "semiannual", intervalDays: 180, discountPercent: Number(values.semiannualDiscount) }, { cycle: "annual", intervalDays: 365, discountPercent: Number(values.annualDiscount) }] })} /></PanelLayout>;
}
