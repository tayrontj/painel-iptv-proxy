name = "videlis-scheduler"
main = "src/index.ts"
compatibility_date = "2026-08-27"

[triggers]
crons = ["*/5 * * * *", "0 * * * *"]

# Configure como secrets no painel Cloudflare, nunca neste arquivo:
# wrangler secret put VERCEL_API_BASE_URL
# wrangler secret put NEXUS_SCHEDULER_SECRET
